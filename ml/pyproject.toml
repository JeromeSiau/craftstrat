[project]
name = "oddex-ml"
version = "0.1.0"
description = "XGBoost training and inference utilities for Oddex slot models"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26,<3",
    "xgboost>=2.0,<3",
]

[tool.uv]
package = false
